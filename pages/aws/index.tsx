import { H4, Panel } from '@bigcommerce/big-design';


const Aws = () => {

    return (
        <Panel header="Aws" id="aws">
            <H4>AWS</H4>
        </Panel>
    );
};

export default Aws;
